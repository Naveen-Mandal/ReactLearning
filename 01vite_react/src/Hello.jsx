//the file extension must be .jsx in vite project (not cumplolery when react app is made with npx create-react-app).

function Hello(){   // the functin name must start with capital latter in both projects.
    return(
        <h1>Hello all of you</h1>
    )
}

export default Hello